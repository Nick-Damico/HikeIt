<% if current_user.id == user.id && current_page?(user_path) %>
	<%= link_to "edit", edit_user_path(user), :class => "btn btn-login-form text-white edit-profile-btn" %>
<% end %>

<!-- <%= display_profile_img(user) %> -->
